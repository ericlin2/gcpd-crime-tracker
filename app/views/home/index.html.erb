<div class="col s9">
  <div class="card blue lighten-4">
    <div class="card-content">
      <span class="card-title"><em>Hello.</em></span>
      <% if logged_in? %>
        <p>Welcome, <%= current_user.first_name %>!</p>
      <% else %>
        <p>Please log in to access the GCPD Crime Tracker.</p>
      <% end %>
    </div>
  </div>
</div>