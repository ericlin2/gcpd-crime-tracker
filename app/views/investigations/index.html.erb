<%if @open_investigations.empty?%>
    <h4>No open cases at this time</h4>
<%else%>
    <h4>Open Investigations in GCPD</h4>
    <table class="striped highlight">
    <thead>
        <th>Title</th>
        <th> Crime Location</th>
        <th>Date opened</th>
        <th> Batman Involved? </th>
    </thead>
    <%for i in @open_investigations.chronological.reverse_each%>
        <tr>
            <td><%= link_to i.title, investigation_path(i) %></td>
            <td><%= i.crime_location %></td>
            <td><%= i.date_opened.strftime("%m/%d/%y")%></td>
            <td><%= i.batman_involved ? "Yes" : "No" %></td>
        </tr>
    <% end %>
    </table>
    <br>
<% end%>